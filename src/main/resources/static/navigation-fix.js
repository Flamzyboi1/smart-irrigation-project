(()=>{function show(id){document.querySelectorAll('.view').forEach(v=>v.classList.remove('show'));const target=document.getElementById(id);if(target)target.classList.add('show');document.querySelectorAll('nav button[data-v]').forEach(b=>b.classList.toggle('on',b.dataset.v===id));if(id==='mapview'&&typeof initMap==='function'){initMap();setTimeout(()=>window.map&&window.map.invalidateSize(),100)}}function wire(){document.querySelectorAll('nav button[data-v]').forEach(b=>{b.type='button';b.addEventListener('click',e=>{e.preventDefault();show(b.dataset.v)})})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wire);else wire()})();