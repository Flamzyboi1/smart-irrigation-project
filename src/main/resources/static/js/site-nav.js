(function(){const links=[['Dashboard','/dashboard.html'],['Field map','/field-map.html'],['Field blocks','/field-blocks.html'],['Sensors','/sensors.html'],['Water calculation','/water-calculation.html'],['Crop characteristics','/crop-characteristics.html'],['Cost estimate','/cost-estimate.html'],['Calculator','/calculator.html'],['Alerts','/alerts.html'],['SensLog','/senslog.html']];function add(){document.querySelectorAll('nav,.nav,.navbar').forEach(n=>{links.forEach(([label,url])=>{if(!n.querySelector('a[href="'+url+'"]')){const a=document.createElement('a');a.href=url;a.textContent=label;a.style.marginLeft='12px';n.appendChild(a)}})})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',add);else add()})();